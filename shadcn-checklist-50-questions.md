# Чек-лист: 50 вопросов для работы с shadcn/ui компонентами

## 🔧 Настройка и конфигурация (1-10)

1. ✅ Установлен ли shadcn/ui CLI (`bunx shadcn@latest`) в проекте?
2. ✅ Существует ли файл `components.json` с корректными путями и настройками стиля?
3. ✅ Настроены ли пути импорта компонентов (`@/components/ui/*`) в `components.json`?
4. ✅ Установлены ли зависимости: `@radix-ui/*`, `react-hook-form`, `@hookform/resolvers`, `zod`, `@tanstack/react-form`?
5. ✅ Настроена ли цветовая палитра shadcn/ui в `tailwind.config.js` с CSS переменными?
6. ✅ Импортированы ли шрифты (Inter) и базовые стили в `global.css`?
7. ✅ Настроены ли CSS переменные для радиуса скругления (`--radius`) в конфигурации?
8. ✅ Установлен ли TypeScript с правильными типами для всех shadcn компонентов?
9. ✅ Проверена ли совместимость версий: React 18+, Tailwind CSS 3.3+, TypeScript 5.0+?
10. ✅ Настроена ли интеграция с shadcn MCP сервером в Cursor для быстрой установки компонентов?

## 🎨 Базовые UI компоненты (11-25)

11. ✅ Добавлен ли компонент `Button` со всеми вариантами: default, destructive, outline, secondary, ghost, link,
    loading?
12. ✅ Настроен ли компонент `Input` с поддержкой всех HTML input типов, валидацией и `InputGroup` для сложных полей?
13. ✅ Реализован ли компонент `Card` с `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`?
14. ✅ Добавлен ли компонент `Badge` с цветовыми вариантами и размерами (default, secondary, destructive, outline)?
15. ✅ Настроен ли компонент `Avatar` с изображением, fallback текстом и поддержкой `AvatarGroup`?
16. ✅ Реализован ли компонент `Separator` с ориентацией (horizontal/vertical) для разделения контента?
17. ✅ Добавлен ли компонент `Label` с поддержкой `htmlFor` для ассоциации с form controls?
18. ✅ Настроен ли компонент `Tooltip` с позиционированием (top, right, bottom, left) и анимациями?
19. ✅ Реализован ли компонент `Alert` с типами: default, destructive с иконками и описаниями?
20. ✅ Добавлен ли компонент `Progress` с поддержкой неопределенного состояния и кастомных значений?
21. ✅ Настроен ли компонент `Skeleton` с различными размерами и анимациями для состояний загрузки?
22. ✅ Реализован ли компонент `Switch` с состоянием on/off, контролем формы и доступностью?
23. ✅ Добавлен ли компонент `Checkbox` с группировкой `CheckboxGroup` и контролем состояния?
24. ✅ Настроен ли компонент `RadioGroup` с `RadioGroupItem` для выбора единственной опции?
25. ✅ Реализован ли компонент `Slider` с диапазонами, шагами и кастомными значениями?

## 📝 Формы и валидация (26-35)

26. ✅ Установлен ли React Hook Form (`useForm`, `Controller`) для управления состоянием форм?
27. ✅ Настроена ли валидация с Zod схемами и `@hookform/resolvers/zodResolver`?
28. ✅ Реализован ли компонент `Form` с `Field`, `FieldLabel`, `FieldDescription`, `FieldError`?
29. ✅ Добавлена ли обработка ошибок валидации с отображением сообщений через `FieldError`?
30. ✅ Настроено ли состояние загрузки для форм с disabled состоянием кнопок и индикаторами?
31. ✅ Реализован ли `Select` с `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem` и поиском?
32. ✅ Добавлен ли `Textarea` с `InputGroupTextarea`, авторазмером и счетчиком символов?
33. ✅ Настроен ли `Calendar` компонент с `Popover` для выбора дат и диапазонов?
34. ✅ Реализован ли компонент `InputOTP` для верификации с масками и разделителями?
35. ✅ Добавлена ли поддержка `InputGroup` с `InputGroupAddon` и `InputGroupText` для сложных полей?

## 🗂️ Навигация и меню (36-42)

36. ✅ Настроено ли `NavigationMenu` с `NavigationMenuList`, `NavigationMenuItem`, `NavigationMenuTrigger`,
    `NavigationMenuContent`?
37. ✅ Реализован ли `Breadcrumb` с `BreadcrumbList`, `BreadcrumbItem`, `BreadcrumbLink`, `BreadcrumbSeparator`?
38. ✅ Добавлен ли `DropdownMenu` с `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`,
    `DropdownMenuSeparator`?
39. ✅ Настроен ли `ContextMenu` с `ContextMenuTrigger`, `ContextMenuContent`, `ContextMenuItem` для правой кнопки мыши?
40. ✅ Реализован ли `Menubar` с `MenubarMenu`, `MenubarTrigger`, `MenubarContent`, `MenubarItem` для desktop?
41. ✅ Добавлены ли `Tabs` с `TabsList`, `TabsTrigger`, `TabsContent` для переключения контента?
42. ✅ Настроена ли пагинация с `Pagination`, `PaginationContent`, `PaginationItem`, `PaginationLink`?

## 📊 Данные и таблицы (43-47)

43. ✅ Реализована ли `Table` с `TableHeader`, `TableBody`, `TableRow`, `TableCell`, `TableCaption` и сортировкой?
44. ✅ Добавлен ли `DataTable` компонент с колонками, фильтрацией, поиском и экспортом данных?
45. ✅ Настроены ли графики с `Chart` компонентом, поддержкой различных типов диаграмм (bar, line, pie)?
46. ✅ Реализован ли компонент `Empty` для отображения пустого состояния с иконками и сообщениями?
47. ✅ Добавлена ли виртуализация с `ScrollArea` для больших списков с оптимизацией производительности?

## 🎯 Продвинутые компоненты (48-50)

48. ✅ Настроен ли `Dialog` с `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`,
    `DialogFooter`?
49. ✅ Реализован ли `Drawer` с `DrawerTrigger`, `DrawerContent`, `DrawerHeader`, `DrawerTitle`, `DrawerDescription` для
    мобильной навигации?
50. ✅ Добавлена ли система уведомлений с `Sonner` и `toast()` функцией для различных типов уведомлений?

## 📋 Дополнительные вопросы для аудита

### Производительность

- Оптимизированы ли компоненты с `React.memo` и `useMemo` для предотвращения лишних рендеров?
- Используется ли `React.lazy` и `Suspense` для code splitting тяжелых компонентов?
- Настроено ли tree shaking для исключения неиспользуемых стилей из финальной сборки?

### Доступность (A11y)

- Добавлены ли ARIA атрибуты (`aria-label`, `aria-describedby`, `aria-invalid`) к интерактивным элементам?
- Поддерживается ли полная навигация с клавиатуры (Tab, Enter, Space, Arrow keys)?
- Настроены ли screen reader описания с `aria-live` регионами и `role` атрибутами?

### Темизация

- Реализована ли темная тема с `next-themes` и CSS переменными (`--background`, `--foreground`)?
- Настроены ли семантические цветовые токены (primary, secondary, muted, accent, destructive)?
- Поддерживается ли автоматическое переключение темы на основе системных настроек (`prefers-color-scheme`)?

### Тестирование

- Написаны ли unit тесты с `@testing-library/react` для компонентов и их пропсов?
- Добавлены ли интеграционные тесты для сложных форм с `react-hook-form` валидацией?
- Настроено ли визуальное тестирование с `@playwright/test` для кросс-браузерной совместимости?

### Документация

- Создана ли Storybook документация с Controls addon для интерактивного тестирования компонентов?
- Добавлены ли живые примеры использования в registry с `form-rhf-demo`, `button-demo` паттернами?
- Написаны ли README файлы с инструкциями по установке, API и примерами использования?

## 🛠️ Инструменты и автоматизация

### MCP интеграция

- Настроен ли shadcn MCP сервер в `.cursor/mcp.json` с `bunx shadcn@latest mcp` командой?
- Работает ли автоматическая установка компонентов через команды типа "Добавить button, form, input"?
- Поддерживается ли поиск по реестрам с фильтрацией по типам (ui, block, example)?

### CI/CD

- Настроена ли автоматическая проверка с `eslint`, `typescript`, `tailwindcss` линтерами?
- Добавлена ли валидация типов с `tsc --noEmit` в GitHub Actions pipeline?
- Настроено ли автоматическое развертывание компонентов через npm publish с changeset?

### Мониторинг

- Настроена ли аналитика использования компонентов с custom events для отслеживания UX метрик?
- Добавлен ли мониторинг ошибок с Sentry или аналогичными инструментами для production?
- Реализованы ли метрики производительности с Web Vitals для Core Web Vitals показателей?

---

**Инструкция по использованию:**

- Отмечайте вопросы по мере выполнения ✅
- Для каждого компонента проверяйте все связанные вопросы
- Используйте MCP сервер для быстрой установки недостающих компонентов
- Регулярно обновляйте компоненты до последних версий
- Проводите аудит доступности после каждого крупного обновления

**Полезные команды MCP:**

- `Показать все доступные компоненты в shadcn registry`
- `Добавить button, form, input, card компоненты в проект`
- `Найти компоненты для форм в shadcn registry`
- `Создать контактную форму используя компоненты shadcn`
- `Показать примеры использования button компонента`
